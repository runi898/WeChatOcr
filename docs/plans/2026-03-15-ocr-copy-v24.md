# OCR Copy Cleanup Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Unify screenshot copy and translation OCR text cleanup, remove overlay hint text, and ship `WeChatOCR_Tool_v24.exe`.

**Architecture:** Introduce a pure OCR post-processing helper with tests, route both UI flows through it, then update packaging metadata for a new release build.

**Tech Stack:** Python 3, tkinter, wcocr, unittest, PyInstaller

---

### Task 1: Add OCR post-processing tests

**Files:**
- Create: `Z:\share\WeChatOCR\.worktrees\ocr-copy-v24\test_ocr_postprocess.py`

1. Write failing tests for same-line English fragment merge, same-line Chinese fragment merge, and top-to-bottom line ordering.
2. Run `python -m unittest test_ocr_postprocess.py` and confirm import or behavior failure.
3. Implement the minimal helper module to satisfy the tests.
4. Re-run `python -m unittest test_ocr_postprocess.py` and confirm pass.

### Task 2: Integrate shared OCR cleanup into app flows

**Files:**
- Create: `Z:\share\WeChatOCR\.worktrees\ocr-copy-v24\ocr_postprocess.py`
- Modify: `Z:\share\WeChatOCR\.worktrees\ocr-copy-v24\screenshot_tool.py`

1. Replace `_run_ocr_only()` raw line join with the shared helper output.
2. Replace `_run_ocr_translate()` inline `merged_res` logic with the shared helper output.
3. Keep translation popup behavior unchanged apart from the cleaned text source.
4. Run targeted tests plus `python -m py_compile screenshot_tool.py ocr_postprocess.py`.

### Task 3: Remove capture overlay hint and package v24

**Files:**
- Modify: `Z:\share\WeChatOCR\.worktrees\ocr-copy-v24\screenshot_tool.py`
- Create: `Z:\share\WeChatOCR\.worktrees\ocr-copy-v24\WeChatOCR_Tool_v24.spec`

1. Remove the center hint text from `grab_region()` for all modes.
2. Copy `WeChatOCR_Tool_v23.spec` to `WeChatOCR_Tool_v24.spec` and update the executable name.
3. Run full verification:
   - `python -m unittest test_ocr_postprocess.py test_translation_layout.py`
   - `python -m py_compile screenshot_tool.py ocr_postprocess.py translation_layout.py test_ocr_postprocess.py test_translation_layout.py`
   - `pyinstaller WeChatOCR_Tool_v24.spec`
4. Confirm `dist/WeChatOCR_Tool_v24.exe` exists.
